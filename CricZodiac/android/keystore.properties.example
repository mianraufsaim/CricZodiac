# Keep the real keystore.properties private. Do not commit it.
# This path is resolved from the android directory.
storeFile=/absolute/path/to/criczodiac-upload.jks
storePassword=replace-with-keystore-password
keyAlias=criczodiac-upload
keyPassword=replace-with-key-password
